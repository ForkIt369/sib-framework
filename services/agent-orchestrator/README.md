# services/agent-orchestrator

Non-authoritative planning layer that converts world observations into bounded action requests.

Responsibilities:
- Policy-constrained intent planning.
- Model routing (local vs frontier models).
- Retry and fallback behavior.
